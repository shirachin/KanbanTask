# デプロイ先のIPアドレス（localhost または サーバーのIPアドレス）
DEPLOY_IP=localhost

# フロントエンドのポート番号
FRONTEND_PORT=5173

# バックエンドのポート番号
BACKEND_PORT=8001

# プロキシ設定（必要に応じて設定）
# HTTPプロキシ（例: http://proxy.example.com:8080）
HTTP_PROXY=

# HTTPSプロキシ（例: http://proxy.example.com:8080）
HTTPS_PROXY=

# プロキシを経由しないホスト（カンマ区切り、例: localhost,127.0.0.1,.local）
NO_PROXY=localhost,127.0.0.1,.local

# データベース設定（他のアプリと競合する場合に変更）
# データベースユーザー名（デフォルト: taskapp）
DB_USER=taskapp

# データベースパスワード（デフォルト: taskapp_password）
DB_PASSWORD=taskapp_password

# データベース名（デフォルト: taskapp_db）
DB_NAME=taskapp_db

# データベースデータの保存パス（デフォルト: ./data/postgres）
DB_DATA_PATH=./data/postgres

# データベースのポートをホスト側に公開する場合（デフォルト: 非公開）
# バックエンドコンテナからは db:5432 でアクセス可能（Dockerネットワーク経由）
# 外部から直接アクセスする必要がある場合のみ docker-compose.yml の ports を有効化してください
# DB_PORT=5432
